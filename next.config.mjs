/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['images.unsplash.com','gateway.pinata.cloud'], // Add the allowed domains here
    },
  };
  
  export default nextConfig;
  